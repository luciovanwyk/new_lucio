model SupportMessage {
    id        String   @id @default(uuid())
    userId    String?
    name      String
    email     String
    title     String
    message   String
    createdAt DateTime @default(now())
    user      User?    @relation("UserToSupportMessages", fields: [userId], references: [id]) // Relation to User

    @@map("support_messages")
}